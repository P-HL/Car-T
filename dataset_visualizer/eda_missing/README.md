# eda_missing.py 精简修改总结


### ✅ 组织结构
1. **静态数据分析**: `analyze_static_data()` 函数 - 100% 保留
2. **动态数据分析**: `analyze_dynamic_data_by_category_dual_pane()` 函数 - 100% 保留
3. **核心计算**: `compute_missing()` 函数 - 100% 保留
4. **数据处理逻辑**: 所有算法完全保留
5. **可视化生成**: 所有图表生成逻辑完全保留
6. **输出格式**: 文件名和格式完全保留

### 分析流程（未变）
```
1. 静态数据分析
   ├─ 读取 CSV 文件
   ├─ 计算缺失值统计
   ├─ 生成条形图和数据表
   └─ 保存 static_data_missing_analysis.png

2. 动态数据分析
   ├─ 读取多个患者 CSV 文件
   ├─ 按类别合并数据
   ├─ 为每个类别计算缺失值
   ├─ 生成双面板图表
   └─ 保存各类别图表
      ├─ dynamic_data_missing_CBC.png
      ├─ dynamic_data_missing_Inflammatory_Biomarker.png
      ├─ dynamic_data_missing_VCN.png
      ├─ dynamic_data_missing_Lymphocyte_Subsets.png
      ├─ dynamic_data_missing_Coagulation.png
      ├─ dynamic_data_missing_Electrolytes.png
      ├─ dynamic_data_missing_Biochemistry.png
      └─ dynamic_data_missing_Vital_Signs.png
```

## 🚀 使用方法

### 修改路径配置
在 `eda_missing.py` 的 main 函数中修改以下三个路径变量：

```python
# ==================== 配置路径 ====================
# 静态数据文件路径
STATIC_DATA_PATH = "/your/path/to/encoded_standardized.csv"

# 动态数据文件夹路径
DYNAMIC_DATA_FOLDER = "/your/path/to/processed_standardized"

# 输出文件夹路径
OUTPUT_FOLDER = "/your/path/to/output"
# ==================================================
```

### 运行脚本
```bash
cd /home/phl/PHL/Car-T/dataset_visualizer/eda_missing
python eda_missing.py
```

### 输出结果
脚本执行后会在 `OUTPUT_FOLDER` 中生成：
- 1 个静态数据分析图表
- 8 个动态数据类别图表（根据数据可用性）

### 路径说明
- **STATIC_DATA_PATH**: 静态数据 CSV 文件的完整路径
- **DYNAMIC_DATA_FOLDER**: 包含多个患者动态数据 CSV 文件的文件夹路径
- **OUTPUT_FOLDER**: 分析结果图表的输出文件夹路径

## ⚠️ 注意事项

### 1. 路径配置
- ✅ 使用**绝对路径**避免路径解析问题
- ✅ 确保路径中的文件夹存在
- ✅ 输出文件夹会自动创建（如果不存在）

### 2. 数据格式要求
- **静态数据**: CSV 格式，第一列为索引
- **动态数据**: 文件夹中包含多个 `.csv` 文件，每个文件代表一个患者

### 3. 修改路径
- 每次更改数据源或输出位置时，需要编辑 `eda_missing.py` 中的路径变量
- 修改后无需重新编译，直接运行即可


